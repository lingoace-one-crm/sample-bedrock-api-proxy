.claude/
# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.env
# Deploy-time secrets. `.env` above does not cover these — gitignore patterns
# match whole names, not prefixes. Keep the .example files tracked so the
# required variables stay discoverable.
.env.local
.env.*.local
!.env.local.example
.pytest_cache
.coverage
htmlcov
.claude
.vscode/
tests_bak/
node_modules/
cdk/cdk.context.json
.worktrees/

# Local tool caches
.codegraph/
.mypy_cache/
.ruff_cache/
.github/
.idea

cdk/scripts/
